"""Property tests for the indicator library (ROADMAP P2-T7).

Currently: truncation-invariance (no-lookahead) property tests, one per
registered indicator, all marked ``lookahead``. See test_indicator_truncation.py.
"""
