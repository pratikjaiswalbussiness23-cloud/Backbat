# OPEN_QUESTIONS.md

Per ROADMAP.md §2A (R7): if anything is ambiguous or a test seems wrong, STOP and log the question here instead of silently choosing an interpretation. An agent must never resolve a row's Status itself; the owner does.

| ID | Question | Raised by | Status |
|----|----------|-----------|--------|
| OQ-0001 | `NLBT_LLM_MODEL` default: the task mandates `claude-3-5-sonnet-20241022`. Verified via Anthropic's official model-deprecations page (platform.claude.com/docs/en/about-claude/model-deprecations, fetched 2026-09-19): the ID is real but **retired** from the first-party API. Keep as-is per task text, or switch default to a live model (e.g. a claude-sonnet-4.x ID)? Not decided silently because P6-T6 (live LLM calls) would fail at runtime with the retired default. | P0-T4 agent (Buffy) | Open |
| OQ-0002 | Should unknown `NLBT_*` env vars fail loudly? Empirically verified (2026-09-19, installed pydantic-settings 2.15.0, including installed source of `sources/providers/env.py`): `extra="forbid"` rejects unknown *constructor kwargs* only — unknown *environment variables* are silently ignored because the env source collects values for known fields. If loud detection is wanted, explicit env scanning must be added (small feature). Task says `extra="forbid"` without specifying env-var scope. | P0-T4 agent (Buffy) | Open |
| OQ-0004 | `pandas-stubs` (2.3.3.260113) was added to the dev dependency group for P1-T1/T2 — mypy strict mode refuses pandas without stubs (`import-untyped` + `Class cannot subclass DataFrame`). Not in the task's allowed file list; flagging per R7/R12 instead of working around with blanket ignores. | P1-T1/T2 agent (Buffy) | Open |
| OQ-0005 | P1-T1 task text says "EXACTLY these lowercase columns" — implemented as an error on EXTRA columns as well as missing ones (strict reading: extras would silently flow into the engine otherwise). If providers legitimately attach auxiliary columns later, this must be relaxed deliberately. | P1-T1/T2 agent (Buffy) | Open |
| OQ-0006 | `yfinance>=1.7,<2` added to `[project] dependencies` for P1-T3 — not in the task's allowed file list (same situation as OQ-0004). The task mandates probing the INSTALLED yfinance, which required installing it. Also pulls in `requests`, `curl_cffi`, etc. (transitively locked). | P1-T3 agent (Buffy) | Open |
| OQ-0007 | Task mandates `@pytest.mark.unit` on all test_yf_provider.py tests, but `unit` is not a registered marker and pytest runs with `--strict-markers` (P0-T2) — unregistered markers are hard errors. Registered `unit: pure-unit test, no network/LLM` in pyproject `[tool.pytest.ini_options] markers` to comply. Owner may prefer a different convention (e.g. only `network`/`llm_live` registered and `unit` implied). | P1-T3 agent (Buffy) | Open |
| OQ-0008 | P1-T5 `large_return_bar.csv` as literally specified (close=160 with high=105) is internally inconsistent: 160 > high trips the BLOCKING `HIGH_LOW_VIOLATION` check, so the LARGE_SINGLE_RETURN warning could never fire. Task arithmetic ("return from 100 open = 60%") also defines the return as intra-bar |close/open−1|, not close-to-close. Resolution implemented (needs owner confirmation): fixture high raised 105→161 (minimal consistent value, close values exact as mandated); return defined intra-bar. | P1-T5 agent (Buffy) | Open |
| OQ-0009 | P1-T5 `ohlc_violation.csv` (high=98 < low=99) violates BOTH the new max/min rule and validate.py's older `high < low` rule. For the report to name `HIGH_LOW_VIOLATION` (task's required code), the new checks must run BEFORE validate-reuse delegation. Precedence implemented: max/min violations → validate-reuse → warnings. Also: `blocked_by` mixes codes (HIGH_LOW_VIOLATION) with free text (validator messages) — kept per task text. | P1-T5 agent (Buffy) | Open |
| OQ-0009b | P1-T5 tests 9/14 demand "zero warnings" for valid_daily.csv, but STALE_LAST_BAR uses `today`; with the real clock (2026) every 2024 fixture is stale. All stale-sensitive tests inject `today=date(2024,1,20)` (task's own injection mechanism) so the default-clock case is never load-bearing. | P1-T5 agent (Buffy) | Open |
| OQ-0011 | P1-T6 defines `trading_days_per_week` as the **mean** bars per ISO week, but its own test 8 (30 consecutive daily bars → DAILY_CRYPTO) is unsatisfiable with a mean: 30 consecutive days always touch ≥ ceil(30/7) = 5 ISO weeks, so mean ≤ 30/5 = 6.0 < the 6.5 crypto threshold. Probed on installed pandas 3.0.6 (2026-09-20): counts `[7, 7, 7, 7, 2]` → mean 6.0, median 7.0. Resolution implemented (needs owner confirmation): `trading_days_per_week` = **median** of per-ISO-week counts — robust to the unavoidable partial edge weeks, exact for regular patterns, and satisfies every mandated test. | P1-T6 agent (Buffy) | Open |
| OQ-0012 | P1-T6's classification maps `trading_days_per_week >= 3.0` → WEEKLY and else → UNKNOWN, but its own test 3 (one bar per week, ~1.0 bars/week → WEEKLY) contradicts that: 1.0 < 3.0 → UNKNOWN/252, which also breaks ROADMAP P1-T6's acceptance criterion "weekly → 52". Resolution implemented (needs owner confirmation): a branch `median_gap_days >= 5.5` → WEEKLY/52 (the ROADMAP P1-T6 Do line explicitly sanctions "median bar spacing + weekday pattern" as the inference signals; 5.5 separates sparse-daily, whose median gap is ≤ 3.5 for even a Tue/Thu pattern, from true weekly bars at exactly 7). The `else` → UNKNOWN/252 branch is preserved for erratic sparse patterns (e.g. 2 bars/week with ~3.5-day median gap). Sub-point: with an override AND < 2 bars, the override wins for bars_per_year (§4.7 "explicitly supplied") while the insufficient-bars warning is still set — the task's edge-case and override branches can otherwise not both hold. | P1-T6 agent (Buffy) | Open |
| OQ-0013 | P2-T1 task text pins the pydantic class-kwargs form `class ParamSpec(BaseModel, extra="forbid")`, but the codebase convention (since P0/P1) is `model_config = ConfigDict(extra="forbid")`. Probed on installed pydantic 2.13.5 (2026-09-20): BOTH forms are runtime-equivalent (extra kwargs rejected either way). Resolution implemented (needs owner confirmation): the ConfigDict form, because mypy strict mode runs WITHOUT the pydantic mypy plugin and rejects class-kwargs as an unexpected keyword argument. If the owner prefers the literal task form, the fix is enabling `plugins = ["pydantic.mypy"]` in `[tool.mypy]` — a config change outside this task's file list. | P2-T1 agent (Buffy) | Open |
| OQ-0014 | P2-T1 specifies `validate_indicator_params` error behavior only for unknown params and bound violations ("Raises E_SPEC_RANGE with details on any violation"). Three unspecified surfaces were resolved as follows (needs owner confirmation): (1) unknown INDICATOR name → `SpecRefError` (E_SPEC_REF, §4.8 row 2 "Unknown indicator/operand/output reference") instead of leaking `Registry.get`'s raw `KeyError` — a bare KeyError would escape the NlbtError taxonomy that P6 repair loops catch; (2) non-numeric / non-finite (NaN, ±inf) / bool / float-for-`type="int"` values also raise E_SPEC_RANGE ("any violation"), NOT silent pydantic coercion — probed: pydantic 2.13.5 coerces `True` → `1.0` on `float \| int`, which would silently change strategy semantics; (3) bounds are INCLUSIVE (`minimum <= value <= maximum`), matching the spec schema's `minimum`/`maximum` wording. | P2-T1 agent (Buffy) | Open |
| OQ-0015 | P2-T2 mandates the oracle library `pandas-ta`, but it is UNINSTALLABLE in this project: pandas-ta pins `numba==0.61.2`, which requires `numpy>=1.24,<2.3`, while nlbt requires `numpy>=2.5` (uv resolution error reproduced in DEPENDENCIES.md). Resolution implemented (needs owner confirmation): oracle tests use `ta==0.11.0` (bukosabino, MIT, dev-only, pure pandas — installs cleanly), with the mandated probe output recorded verbatim in verified_apis.md. If the owner insists on pandas-ta, the options are (a) downgrade numpy project-wide (rejected by the P1 agent: destabilizes the whole data layer for a dev-only oracle) or (b) wait for a pandas-ta release that drops the numba pin. | P2-T2 agent (Buffy) | Open |
| OQ-0016 | P2-T2 says "Handle NaN in input: if any of the seed window is NaN, the seed fails and EMA stays NaN until a valid window exists" but does not define behavior AFTER the seed when a later input is NaN. Resolution implemented (needs owner confirmation): reseed on the next fully-valid window (a NaN at step t poisons only t and t+1 — the values reachable from the contaminated ema[t-1]) rather than staying NaN forever, because §4.3 requires "no NaN after warm-up unless the input is NaN" and Bars validation already blocks NaN OHLC upstream; the hand-computed test 8 passes under both readings, so it does not arbitrate. If the owner prefers permanent-NaN semantics, change the seed search to `if first is not None: return` and update §4.3 accordingly. | P2-T2 agent (Buffy) | Open |
| OQ-0017 | P2-T3 test 17 says "If divergence exists: document it, do not hide it" — divergence DOES exist and is structural, not numeric-noise: installed ta 0.11.0 computes RSI via `ewm(alpha=1/period, min_periods=period, adjust=False)` from index 0 (read from installed source; probe recorded in verified_apis.md), so its output is valid from index **period−1** and its early values reflect a phantom-zero seed, NOT Wilder's SMA seed. Our ADR-0003 implementation is valid from index **period**. Consequences (needs owner confirmation): (a) test 17 is a divergence-documentation test asserting both sides' exact values plus convergence (probed: max diff after bar 100 ≈ 0.000–0.006 for period 5/14 on a 300-bar random walk); (b) the ROADMAP P2 acceptance "matches a reference library after the documented convergence window" is satisfiable only from ~bar 30–100 onward, which the test states explicitly. If the owner wants EARLY bars to match ta, the convention must change to ta's ewm seeding — that would contradict Wilder 1978 and ADR-0003. | P2-T3 agent (Buffy) | Open |
| OQ-0018 | P2-T3 test 18 claims "ta does not have ROC directly" — FALSE for ta 0.11.0: `ta.momentum.ROCIndicator(close, window).roc()` exists and matches the task's own hand computation exactly on the probe series ([nan, nan, nan, 30.0, 27.2727…, 25.0, 23.0769…]). Resolution implemented (needs owner confirmation): test 18 uses BOTH the hand-computed values (primary oracle) AND the ta ROCIndicator cross-check (recorded in verified_apis.md). Risk if owner disagrees: none — the ta cross-check can be dropped without weakening the hand-computed assertions. | P2-T3 agent (Buffy) | Open |
| OQ-0019 | P2-T3's file list says to modify `docs/OPEN_QUESTIONS.py` — no such file exists; the project's open-questions register is `docs/OPEN_QUESTIONS.md` (used by every task since P0). Assumed `.py` is a typo and modified the `.md` file. | P2-T3 agent (Buffy) | Open |
| OQ-0020 | ADR-0004's Consequences say ddof=1 "Matches ta library and most charting platforms", but installed ta 0.11.0 uses ddof=0 (read from `BollingerBands._run` source; probe arithmetic in verified_apis.md proves √(665/20), not √(665/19)). Resolution implemented (needs owner confirmation): keep the task-mandated ddof=1 (Wilder-style sample convention; the ADR's Decision text stands), record the ta divergence in the ADR's implementation notes, and restrict the ta BBands cross-check to the middle column (SMA, exact match). If the owner wants ta-exact bands, switch to ddof=0 — a one-line change plus test updates. | P2-T4 agent (Buffy) | Open |
| OQ-0021 | P2-T4 mandates param bounds per-indicator (fast/slow/signal each [2,200]) but not the cross-param RELATION the MACD definition requires: the warmup formula `slow + signal - 2` and the whole concept of a fast/slow pair presuppose fast < slow. If fast >= slow (e.g. fast=50, slow=10 — both individually valid), the indicator still computes but semantically degenerates. Resolution implemented (needs owner confirmation): bounds validated exactly as mandated (no extra rejection); the ambiguity is logged here for the spec layer (P3) which must decide whether E_SPEC_RANGE should cover cross-param constraints. A cheap partial guard would be a registration-time check `fast_default < slow_default` (12 < 26 holds). | P2-T4 agent (Buffy) | Open |
| OQ-0022 | P2-T5 mandates ADX `warmup = 2*period` and tests 12/26/27 hard-code the consequences (ADX[0..2·period−1] NaN; `warmup_fn == 2*period`), but the task's own cascade arithmetic contradicts that: DX is valid from index `period` (DI needs the running sums, seeded from the first `period` values), so the ADX seed — the SMA of the FIRST `period` DX values — lands at index `2·period − 1`, not `2·period`. The ta 0.11.0 probe (recorded in verified_apis.md BEFORE coding) confirms: first REAL ADX value at index 5 for window=3 (ta's earlier values are np.zeros fillers from its source). Resolution implemented (needs owner confirmation): `warmup_fn` returns `2*period − 1`; tests 12/26 assert the corrected indices with the arithmetic in the docstrings. If the owner insists on 2·period, the change is one line in `_adx_warmup` plus the seed search start — but then warmup would NOT match the first-valid-index contract used by every other indicator. | P2-T5 agent (Buffy) | Open |
| OQ-0023 | P2-T5 test 14 asserts `plus_di[period] == 100.0` from the premise "+DM = TR always" on its own uptrend fixture, but the task's own tables give +DM = 1 and TR = 2 for every bar, so +DI = 100·(1/2) = **50.0**, not 100.0. The ta 0.11.0 probe confirms 50.0 (recorded in verified_apis.md). Resolution implemented (needs owner confirmation): the hand-computed test asserts +DI = 50.0 with the full cascade arithmetic (sums 3/6 → 100·3/6) in the docstring; test 15 (−DI = 0.0) and test 13 (ADX = 100.0, DX = 100·|50−0|/50) are unaffected. If the owner wants +DI = 100.0, the DM definition itself must change (e.g. +DM normalized by TR per bar), which contradicts the task's stated formula. | P2-T5 agent (Buffy) | Open |
| OQ-0024 | P2-T5's Stochastic convention says "smooth_period applies SMA smoothing to %K before %D" and probes ta with smooth_window=1, but ta 0.11.0 (source read, verified_apis.md) NEVER smooths %K: its `smooth_window` parameter is an SMA over %K producing %D, i.e. ta's `smooth_window` ≡ our `d_period`, and there is no parameter in ta corresponding to our `smooth_period`. Resolution implemented (needs owner confirmation): oracle test 20 compares against ta(window=3, smooth_window=3) ≡ ours(k=3, smooth=1, d=3) — the only parameterization where the two libraries compute the same objects; the mandated smooth-before-D behavior is pinned by the hand-computed `test_stoch_smooth_k_before_d` instead. Also noted: ta's flat-window %K is raw 0/0 → NaN (rewritten to 50 only by its fillna path), while ours is 50.0 by convention per the task text. | P2-T5 agent (Buffy) | Open |
| OQ-0025 | P2-T6's breakout test (test 11) mandates the fixture `high=[10,11,12], close=[9,10,15]` — bar 2 has close=15 > high=12, an internally inconsistent OHLC bar that Bars validation would REJECT upstream (the P1 OHLC checks). The design point ("close[t] > highest[t] uses only past highs, so the signal CAN fire without lookahead") does not need an invalid bar. Resolution implemented (needs owner confirmation): the fixture is adjusted to `high=[10, 11, 12.5], close=[9, 10, 12.4]` — every bar satisfies close <= high, highest[2] = max(high[0], high[1]) = 11.0 is UNCHANGED, and close[2] = 12.4 > 11.0 still fires the breakout. The task's hand arithmetic (test assertion values) is identical; only the demonstration bar became validation-clean. | P2-T6 agent (Buffy) | Open |
| OQ-0026 | P2-T6's test 11 claims the ta OBV oracle should match "exactly (OBV is deterministic, no convention choices)" — FALSE, and the task's own probe disproves it: ta 0.11.0 (source read: `np.where(close < close.shift(1), -volume, volume).cumsum()`) ADDS volume on flat closes, giving [100, 300, 150, 450, **550, 300, 700**] on the task's own 7-value series, while the task's convention (flat → carry previous OBV, classic Wilder OBV) gives [100, 300, 150, 450, **450, 200, 600**]. Resolution implemented (needs owner confirmation): ours follows the task text (neutral on flats; OBV[t] == OBV[t-1] when close[t] == close[t-1]); the oracle test asserts agreement on the strictly-trending prefix (bars 0-3) and documents/asserts the flat-handling divergence at bars 4-6. If the owner wants ta-exact OBV, change one comparison (`<=` vs `<` on the down side) — but that contradicts the task's own stated convention. | P2-T6 agent (Buffy) | Open |
| OQ-0027 | P2-T7 anomaly, NOT a counterexample and NOT suppressed: the FIRST full-suite run after adding tests/property/ showed `tests/unit/test_logging_config.py::test_canary_filter_on_foreign_preexisting_handler` FAIL once; it passes standalone, passes alongside the new property file, and passed in the immediate re-run of the full suite (359 passed). No counterexample was ever raised against any indicator — the truncation property held for all 12 across every run. Evidence points to pre-existing cross-file order/state interference in the P0 logging test (its module name itself suggests ordering sensitivity), unrelated to P2-T7; logged per AGENTS.md ("state plainly what you could not verify") rather than hidden. If it recurs, the owner should consider `pytest-randomly`-style bisection or a fixture-isolation audit of test_logging_config.py. | P2-T7 agent (Buffy) | Open |
| OQ-0028 | P2-T7's params guidance conflicts with its own NaN-aware comparison on RECURSIVE indicators: the comparison must treat one-sided NaN (short-side NaN, long-side value) as a FAILURE (canary premises), so outputs must be valid from the SAME index on bars[:n] and bars[:n+k] — true only when the drawn params keep warmup below the split floor. Drawing params from the FULL registry bounds (e.g. macd slow+signal-2 ≤ 398) against task-fixed bar floors (60/80) makes most draws unsatisfiable (split range inverts → hypothesis `InvalidArgument` churn; reproduced: MACD InvalidArgument in 71.83s with zero completed examples). Resolution implemented (needs owner confirmation): params drawn FIRST, bars length floored per-draw at max(task floor, warmup+4), splits drawn from the registry's warmup_fn (no duplicated warmup logic); several per-indicator param caps were tightened vs registry (documented per strategy, e.g. sma/ema/rsi/atr/adx ≤ 30, bbands period ≤ 100, highest/lowest ≤ 60) while macd KEEPS the full registry bounds and instead takes longer bars — a deliberate split between "cap params" vs "grow bars" so at least one indicator exercises the deep-warmup path. | P2-T7 agent (Buffy) | Open |
| OQ-0029 | The P2-T7 canary spec's demonstration arithmetic is off by one against its own fixtures: prices = list(range(1, 31)) means close[j] = j+1 (VALUES 1..30), so the centered window of position i covers values i-1 .. i+3 — position 13 = 14.0 (70/5), position 14 = 15.0 (75/5) on the long frame, NOT "n-3..n" = positions 12-14 as the task asserts (position 12 = 13.0 = 65/5 is IDENTICAL on both frames; only 13/14 differ; values verified live against pandas before asserting). The task's `any(... for i in range(n-3, n))` check also fails to handle the one-sided-NaN case: a `abs(s-l) > 1e-10` test with NaN on the short side evaluates False (NaN comparisons are False) and would silently SKIP the leak. Resolution implemented (needs owner confirmation): differing positions pinned as exactly {13, 14}; the comparison helper is NaN-ASYMMETRIC (NaN==NaN passes, one-sided NaN raises) so the leak cannot hide behind a NaN; the mandated `range(n-3, n)` scan is preserved verbatim inside test_canary_leaky_indicator_is_caught and fails exactly as intended. | P2-T7 agent (Buffy) | Open |
| OQ-0030 | P2-T8's `__main__` block says the files go to "their standard locations (docs/indicators.md and docs/indicators.json)" without defining the anchor. `uv run python -m nlbt.indicators.generate_docs` from nlbt/ would put them in nlbt/docs/ either way, but the anchor matters when the module is run from elsewhere (e.g. repo root `python -m` on a path, or a CI step with a different cwd). Resolution implemented (needs owner confirmation): paths anchored to the PROJECT ROOT derived from `__file__` (parents[3] = nlbt/), NOT the CWD — regeneration is cwd-independent. The task's staleness test command `uv run python -m nlbt.indicators.generate_docs` works unchanged (uv runs from nlbt/). If the owner prefers CWD-relative `Path("docs")`, flip the two DEFAULT_* constants — but then a wrong-cwd invocation silently writes docs/ somewhere else. | P2-T8 agent (Buffy) | Open |
| OQ-0031 | P2-T8's markdown example shows integer param bounds ("Min: 2, Max: 500"), but ParamSpec (P2-T1) stores bounds as `float` — `minimum=2` becomes `2.0` in the ParamSpec dump, so the generated tables/JSON show `2.0/500.0` (probed: `json_schema()['indicators']['sma']['params']['period']['minimum'] == 2.0`). The task's JSON schema block only mandates keys, not value types, and its own test 4 checks presence of `minimum`/`maximum` only; the md test (test 6) greps the header, not cell formats. Resolution implemented (needs owner confirmation): render the ParamSpec values verbatim (`2.0`) instead of reformatting to int when integral — reformatting would make the table LIE about what the validator enforces (float comparison, `2 <= period` per validate_indicator_params) and duplicate bound logic in the docs layer. If the owner prefers task-example cosmetics, the one-line change is `int(v) if float(v).is_integer() else v` — cosmetic only. | P2-T8 agent (Buffy) | Open |
| OQ-0032 | Global-registry hygiene (pre-existing, REPRODUCED): `tests/unit/test_indicator_registry.py::test_register_indicator_helper_targets_global_singleton` (~line 316) calls `register_indicator(helper_spec)` — permanently adding `dummy_global_path` to the GLOBAL `REGISTRY` with no cleanup. Any later test that reads the live `REGISTRY.list_indicators()` sees 13 indicators depending purely on alphabetical file order. P2-T8 hit this for real: all 6 generation-vs-committed docs tests failed in the full suite while passing 12/12 standalone. The P2-T1 test file is OUTSIDE this task's allowed file list, so it was NOT modified. Resolution implemented (needs owner confirmation): the P2-T8 tests generate their reference artifacts from an IMPORT-TIME snapshot of the registry specs (`BUILTIN_SPECS`, captured at collection before any test runs; C7 intact — still generated from real registry specs), plus `test_registry_still_contains_all_builtins` pinning snapshot≡live-objects. RECOMMENDED owner fix in test_indicator_registry.py: register the dummy into a fresh `Registry()` (the file already has `_fresh_registry_with_dummy`) and pass `registry=reg`, or add an autouse cleanup fixture. | P2-T8 agent (Buffy) | Open |
| OQ-0033 | The P3-T1 task mandates TAGGED discriminated models (models NumberOperand/IndicatorOperand/RawColumnOperand "discriminated on `kind`", rules "recursive AllRule/AnyRule/NotRule with `Node` union"), but the ROADMAP's OWN Appendix C fixtures (the P3-T1 acceptance: "every example in Appendix C parses") are written in the §4.2 SHORTHAND: conditions as bare `{"left": "fast", "op": ..., "right": "slow"}` with NO `kind` tag, rules as `{"all": [...]}` with NO `kind` tag, bare-string/bare-number operands, `{"name": <ref>, "lag": n}` dicts without `output`. Probed on installed pydantic 2.13.5 (2026-09-25): a discriminated union reads the tag off the RAW input BEFORE any class validator runs (`union_tag_not_found`), so class-level validators CANNOT inject missing tags — `mode="before"` FIELD validators on the union-typed fields can. Resolution implemented (needs owner confirmation): `mode="before"` lifting in spec/models.py (`_lift_operand`, `_normalize_node`/`_lift_rule`/`_lift_condition`, `Condition._default_kind`) normalises the §4.2 shorthand into the tagged form before discrimination; fully-tagged documents (what P6 will emit) parse unchanged; nothing is silently accepted that the §4.2 grammar rejects (e.g. `{"all": [...]}` with a `kind` key AND a rule key still fails, unknown fields still fail). Alternative rejected: making Appendix C fixtures tagged by editing them — they are the task's mandated verbatim fixtures. | P3-T1 agent (Buffy) | Open |
| OQ-0034 | §4.2 grammar defines `Operand := ... | "indicator.output" | { "name": <ref>, "lag": int >= 0 }` — NEITHER spelling carries an `output` key, and Appendix C uses the bare-name spelling throughout, but the task's IndicatorOperand model description did not state a default. Resolution implemented (needs owner confirmation): `IndicatorOperand.output` defaults to `"value"` (§4.3 lists `value` as the first/primary output of every single-output indicator); `"macd.signal"` strings split at the LAST dot (indicator names cannot contain a dot). Whether the referenced indicator actually HAS the named output is reference resolution — the P3-T2 semantic validator's job against the live registry (§4.1: `indicators` "must not shadow open/high/low/close/volume or reserved words" is enforced at the schema layer; output existence cannot be, the schema has no registry). | P3-T1 agent (Buffy) | Open |
| OQ-0035 | §4.1 table row `unsupported`: "Parser-populated; strict mode requires empty". The P3-T1 task text did not list this constraint among the fields to model, and "strict mode" is not defined for P3-T1 (nothing else can set the flag). Resolution implemented (needs owner confirmation): `StrategySpec._v1_semantics` rejects any non-empty `unsupported` list (with a §4.1 citation in the message) — for a validated StrategySpec, non-strict mode does not exist yet; if the owner later introduces a relaxed parse mode, this check moves behind that flag. | P3-T1 agent (Buffy) | Open |
| OQ-0036 | Scope boundary between schema (P3-T1) and semantic validation (P3-T2): the task mandates IndicatorDef with a `type` field validated against the registry, but P3-T1 has no file-list access to the registry and P3-T2 owns "reference resolution ... param bounds via the registry". Resolution implemented (needs owner confirmation): `IndicatorDef.type` is checked for SHAPE only (`^[a-z][a-z0-9_]{0,31}$`, same identifier grammar as indicator names/operands) plus `source` ∈ {open, high, low, close, volume}; registry existence, param bounds, and cross-field sizing bounds (e.g. percent ≤ 100) are left to P3-T2. Also: `sizing.value > 0` is enforced at the schema layer (task-mandated bound) while method-RANGES (percent: 0 < v ≤ 100) are explicitly NOT — §4.1 says "Range depends on method". | P3-T1 agent (Buffy) | Open |
| OQ-0037 | P3-T2 test 11 mandates "percent_of_equity value=0 → error", but P3-T1's schema ALREADY rejects `sizing.value <= 0` (`Field(gt=0)`, task-mandated bound), so a VALIDATED spec can never reach the semantic branch with value=0. Resolution implemented (needs owner confirmation): the test drives the branch through `Sizing.model_construct(...)` (an unvalidated instance) and the test docstring states this defence-in-depth rationale — validate_spec must hold its invariants even for specs that skipped pydantic (e.g. programmatically mutated models, which ARE mutable). If the owner prefers the branch to be unreachable-in-practice, the test can be inverted to assert pydantic rejects it first — but then semantic item 11 has no coverage at all. | P3-T2 agent (Buffy) | Open |
| OQ-0038 | Task item 14 (test 15) first computes "SMA(200) on a 1-year period → error (warmup=199, estimated bars ~252, valid)" then corrects itself to SMA(300)/6-months. With the task's own heuristic, SMA(200) on 1y is NOT an error: 365 days → int(365*252/365) = 252 estimated bars, warmup 199 < 252 → valid (the parenthetical "(...valid)" concedes this). Resolution implemented (needs owner confirmation): the mandated SMA(300)/6-month test is the primary boundary test; an ADDITIONAL inclusive-boundary test pins the ">=" behaviour with hand arithmetic (start 2023-10-25, end 2025-01-01: 434 days → int(299.81) = 299 bars → 299 >= 299 → error; one more day → 300 bars → valid). The leap year is included in the day counts (2024-02-29 exists). | P3-T2 agent (Buffy) | Open |
| OQ-0039 | Task item 9 says "Compute total_warmup = max warmup across all indicators USED IN THE RULES", but indicators are declared in `spec.indicators` and referenced by NAME from rules; an indicator defined but never referenced still consumes warm-up (its values are computed and its NaN head exists), while an indicator referenced but not defined is ALREADY an E_SPEC_REF (item 4). Resolution implemented (needs owner confirmation): warm-up is the max across ALL DEFINED indicators (spec.indicators) — the superset of "used in rules" for any schema-valid spec, and the only reading that never silently ignores a defined-but-unreferenced indicator whose warm-up could still eat the window. If the owner wants strict "referenced-only" scoping, filter by the operand names collected from the rule tree (the `_walk_operands` helper already collects them). | P3-T2 agent (Buffy) | Open |
| OQ-0040 | Task item 5 says "check operand.output in spec.outputs" — no such attribute exists on StrategySpec or IndicatorDef (probed: IndicatorDef fields are type/params/source). The meaningful reading (consistent with item 1's registry cross-reference and the P2-T1 IndicatorSpec.outputs tuple) is: look up the indicator's TYPE in the registry and check the output against THAT indicator's outputs tuple. Resolution implemented (needs owner confirmation): exactly that, per-operand, with path `rules.<side>...` and hint "Available outputs: [...]". A dotted name reference ("macd.line") is already split by the P3-T1 schema (OQ-0034); the semantic layer validates the resolved (name, output) pair. | P3-T2 agent (Buffy) | Open |
| OQ-0041 | Task item 12 mandates `fixed_shares` with `allow_fractional=False` requiring an integer value, but is silent on `fixed_shares` with allow_fractional=True and on `fixed_cash` fractional values. Resolution implemented (needs owner confirmation): fixed_shares + fractional=True accepts any value > 0 (fractional shares are the point); fixed_cash only requires value > 0 (item 11; cash can be fractional cents — no integer constraint mandated). Also, the method-specific upper bound for risk_per_trade (item 13) is the SAME 0 < v <= 100 as percent_of_equity — one shared branch — and the stop_loss_pct requirement adds an error on path "sizing.method" with the task's exact message. | P3-T2 agent (Buffy) | Open |
| OQ-0042 | P3-T3's "IMPORTANT NOTES" assert "the pydantic models use mode=\"before\" validators to lift shorthand → tagged. So the LLM will produce tagged format (forced by the schema)" — those two clauses point in opposite directions, and the lift validators (OQ-0033) mean pydantic ACCEPTS shorthand, so pydantic cannot be the forcing mechanism. Resolution implemented (needs owner confirmation): choice A as mandated — the GENERATED schema enforces the fully TAGGED wire format only (probed: the Appendix C shorthand document fails jsonschema with e.g. "'all' is a necessary property"/untagged-node errors, test_10b), while pydantic keeps accepting shorthand on INPUT. Forcing is therefore real but comes from the schema, not the models: P6 must emit tagged documents, and Appendix C's shorthand remains a documented input convenience. | P3-T3 agent (Buffy) | Open |
| OQ-0043 | The task mandates `Draft7Validator or later`; pydantic 2.13.5 emits `$defs`/`$ref`/`const`/`oneOf`+`discriminator` (probed, verified_apis.md) — all 2020-12 idioms (Draft 7 would want `definitions`). Resolution implemented (needs owner confirmation): `Draft202012Validator` with `$schema: https://json-schema.org/draft/2020-12/schema` embedded in the generated file ("or later" satisfied; jsonschema 4.26 enforces it natively — probed, including that `format: "date"` is enforced ONLY with an explicit `format_checker=FormatChecker()`, which validate_with_jsonschema therefore always passes). | P3-T3 agent (Buffy) | Open |
| OQ-0044 | OQ-0032's global-registry pollution (a test registers `dummy_global_path` into the GLOBAL REGISTRY with no cleanup, alphabetically BEFORE this file) would, if it ran first in a full-suite run, leak into the injected indicator enum and make the staleness comparison nondeterministic. The P3-T3 file list does NOT include test_indicator_registry.py, so the polluter is untouched. Resolution implemented (needs owner confirmation): tests build a SNAPSHOT `Registry()` at IMPORT time (collection, before any test mutates state) from the then-clean global REGISTRY and pass it explicitly to every generation; the committed file was generated from the clean registry, so the comparison is apples-to-apples. RECOMMENDED owner fix remains the OQ-0032 one (stop mutating the global in test_indicator_registry.py). | P3-T3 agent (Buffy) | Open |
| OQ-0045 | The task's test 7 ("5.0 and 5 produce identical canonical dicts", built via `model_construct(fee_bps=5)`) presupposes the int survives to the JSON. Probed on installed pydantic 2.13.5 (2026-09-25, verified_apis.md): it does NOT — `model_dump(mode="json")` serialises float-TYPED fields as floats even when the attribute holds an int (`model_construct(fee_bps=5)` dumps as `5.0`), so for SCHEMA-typed fields the two specs are identical BEFORE normalisation. The int-vs-float divergence is real only for Any-typed values (indicator `params`, `Assumption.value`), which the dump passes through verbatim. Resolution implemented (needs owner confirmation): `normalise_number` runs recursively anyway (task mandate), making the equivalence hold at BOTH layers — the test asserts the dict identity through the public `canonicalise` path (as mandated) and test_24 pins the params path where normalisation is load-bearing. The hash contract (5 vs 5.0 hash equal) therefore holds regardless of which pydantic serialisation layer would change. | P3-T4 agent (Buffy) | Open |
| OQ-0046 | The task's test 21 asserts whitespace-freeness via `" " not in canonical_json(c1).split('"')[0]` — that only inspects the text before the FIRST double quote, so a space inside any string VALUE (e.g. the name "SMA 50/200 golden cross") would pass unnoticed while a real structural space would also pass if a key preceded it. Resolution implemented (needs owner confirmation): the mandated literal check is kept AND strengthened — the canonical form must additionally equal itself after stripping every space that lies OUTSIDE JSON string literals (helper in the test file), which catches structural whitespace anywhere while correctly allowing spaces inside values. `separators=(",", ":")` guarantees this by construction. | P3-T4 agent (Buffy) | Open |
| OQ-0047 | S8 says "Every spec field appears in the rendering (test that mutates each field and asserts the text changes)", but two fields CANNOT be mutation-tested: `spec_version` (Literal["1.0"], P3-T1 — no second valid value exists) and `execution.fill` (Literal["next_open"] in v1 — same). Resolution implemented (needs owner confirmation): the canary sweeps all 23 user-variable fields and fails naming any non-rendered one; the two Literal-only fields are documented exceptions — `fill` IS rendered ("Fill: next_open (next bar open)", asserted), `spec_version` deliberately is not (the schema pins it; printing a constant adds noise to the confirmation gate). If the owner wants spec_version in the header, it is a one-line template change plus a canary update. Also: the W2 "fewer than 30 expected trades" warning remains engine-owned (P3-T2 item W2), and `validate_spec` inside the renderer can never raise — errors become the VALIDATION ERRORS section, never an exception. | P3-T5 agent (Buffy) | Open |
| OQ-0048 | §4.4 says defaults.py is "the only place defaults live", but copies of the same values already exist elsewhere and the task's own AST scanner is STRUCTURALLY blind to them: (1) models.py class-level pydantic field defaults — `Execution.fee_bps = 5.0`, `slippage_bps = 5.0`, `fixed_fee = 0.0`, `Capital.currency = "USD"`, `Sizing.allow_fractional = True`, `Universe.interval = "1d"` — parse as `ast.AnnAssign` (probed 2026-09-26, verified_apis.md: AnnAssign is NOT ast.Assign), while the mandated scanner matches only simple `Assign` with Name targets, so it can never flag them; (2) the registry carries rsi `default=14` (= DEFAULTS.rsi_period) in momentum.py's ParamSpec. Resolution implemented (needs owner confirmation): P3-T6 implements the scanner EXACTLY as mandated (simple Assign + canary test 47 proving it fires) and touches nothing outside its file list — widening the scan to AnnAssign would have made the suite fail on models.py, forcing either an edit outside the allowed files or a weakened test (both forbidden). Owner options: (a) point models.py field defaults at `DEFAULTS.*` (one import from models to defaults) and extend the scanner to AnnAssign; (b) declare models/registry the wire-format authority and narrow §4.4's "only place" wording; (c) have the P4 parser inject explicit DEFAULTS values before model_validate so the field defaults never fire. Secondary note: `resolve_date_range` follows the task's literal formula `today.year - int(lookback_years)`, so a fractional lookback (e.g. 5.5) truncates to 5 whole years — flagged in case fractional-year semantics were intended. | P3-T6 agent (Buffy) | Open |
| OQ-0049 | P4-T1..T6 test-author stage: the acceptance criteria define behaviour but there is NO engine API to test against (`nlbt.engine` does not exist — deliberately; tests were written without reading/creating any src). Per TDD/R14 the tests therefore PIN the contract the builder must implement: `nlbt.engine.ledger.Ledger(cash, *, fee_bps, slippage_bps, allow_fractional)` with `.cash/.shares/.fees_paid/.warnings`, `buy(shares, price, *, fee_bps=None, slippage_bps=None) -> fee`, `sell(...)` same, `equity(price)`; `nlbt.engine.signals` = `compare(left, op, right)`, `crosses_above(a,b)`, `crosses_below(a,b)`, `lag(values,k)`, `logical_and/or/not(a,b)`, `evaluate_rule(rule, bars, indicators)`; `nlbt.engine.execution` = `apply_slippage(price, side, bps)`, `compute_fee(shares, price, fee_bps, fixed_fee=0.0)`, `resolve_exit_fill(open, level, kind="stop"|"take_profit")`; `nlbt.engine.sizing.compute_shares(method, value, *, equity, cash, price, fee_bps, fixed_fee, allow_fractional, stop_loss_pct)`; `nlbt.engine.risk.RiskManager(entry_price, *, stop_loss_pct, take_profit_pct, trailing_stop_pct, max_holding_bars, entry_bar)` with `.trail_level` and `.check(bar_mapping, index=None) -> ExitDecision|None` (`.price` pre-slippage/None for time_exit, `.reason` from the §4.6 enum); `nlbt.engine.backtest.run(spec, bars) -> result` with `.trades` (§4.6 field names), `.warnings: list[str]`, `.equity_curve: DataFrame[t/equity/cash/position_value/drawdown]`, `.metrics`. Names derive from §3.4 module list + §4.5/§4.6 vocabulary only. Needs owner/builder confirmation: either implement to this surface or propose renames in review (tests must not be edited to mirror the implementation afterwards). | P4-T1..T6 test-author agent (Buffy) | Open |
| OQ-0050 | SC2's printed hand-arithmetic is internally inconsistent — three verified checks (2026-09-26, independent Python calc): `10000 / 101.0505 = 98.9604207…` (text prints 98.9607…); `98.9607 × 101.0505 = 10000.0282…` (text prints 9999.975); `10000 − 9999.975 − 4.999 = −4.974` (text prints "≈ 0"). Its shares formula also omits the fee divisor that §4.5 mandates ("shares = notional / (fill_price × (1 + fee_rate))") — sizing to `10000/101.0505` then charging the fee leaves cash at −5, violating I2/L10. Resolution implemented (needs owner confirmation): test_sc2 asserts §4.5's fee-inclusive chain — shares = 10000/101.10102525 = 98.910965297…, cost 9995.0025, fee 4.9975, cash ≈ 0, equity marks 9990.0075/10088.9185/10187.8294/10286.7404/10385.6514, end-of-data net_pnl 375.2683 — matching the criterion's own intent ("cash_after ≈ 0", "equals buy-and-hold minus costs EXACT"); full arithmetic + the discrepancy proof are in the test docstring. | P4-T1..T6 test-author agent (Buffy) | Open |
| OQ-0051 | SC3 prints `fee_buy = 9904.95 × 0.0005 = 4.95225`, but the product is **4.952475** (verified; the printed value equals 9904.5 × 0.0005 — a digit dropped). The text's downstream chain (cash 90.09775, final 10969.2105, net 969.2105) inherits the slip. Resolution implemented (needs owner confirmation): test_sc3 asserts the arithmetically correct chain — fee_buy 4.952475, cash after entry 90.097525, proceeds 10884.555, fee_sell 5.4422775, final cash 10969.2102475, net_pnl 969.2102475 — each cross-checked two ways in the docstring ((exit−entry)×shares − total fees gives the identical number). | P4-T1..T6 test-author agent (Buffy) | Open |
| OQ-0052 | SC12 says "Exit forced at bar 5 (bar 2 + 3 = bar 5)" while §4.5 step 4 mandates that a `max_holding_bars` exit **creates a pending order filled at t+1**. Two readings: (a) the force/decision happens at bar 5's signal step, fill at bar 6 open; (b) the position exits ON bar 5 itself (intrabar/close). Resolution implemented (needs owner confirmation): reading (a) — test_sc12 pins `exit_signal_time == bars[5]` AND `exit_time == bars[6]` with `exit_price == open[6]`, which satisfies "at bar 5" for the decision while obeying §4.5's fill rule. If the owner wants the exit executed on bar 5, §4.5 step 4 must be amended first (R7) and the test updated with it. | P4-T1..T6 test-author agent (Buffy) | Open |
| OQ-0053 | §4.5 says a position still open after the last bar is "closed at the last close with exit_reason = end_of_data", but does not say (i) whether adverse slippage and fees apply to that forced close, nor (ii) whether the equity curve's LAST row shows the §4.5 step-3 mark (position still open) or the post-close state. Resolution implemented (needs owner confirmation): (i) SC2 applies slippage+fees at the end-of-data fill (consistent with "fees and slippage applied at fill time" and with P4-T8's "buy-and-hold minus costs"), while SC8b deliberately runs slippage_bps=0 so it is agnostic and asserts exit_price == last close literally; (ii) the last row is asserted as the step-3 mark (SC2 equity[5] = 10385.6514 with the position open), the forced close is observed via the trade record, and other tests avoid asserting last-row cash (SC9a checks rows 1-3 only). | P4-T1..T6 test-author agent (Buffy) | Open |
| OQ-0054 | L7 offers "reduce to floor(100/50.025) = 1 share **OR** reject entirely with a warning" — both behaviours are sanctioned by the criteria, so test_l7 accepts EITHER branch but pins exact numbers inside each (reduce: shares == 1, cash == 49.9499875; reject: cash == 100.0, warning present) plus `cash >= 0` unconditionally. Related: L5's `shares = floor(cash / adverse_price)` ignores fees — charging 5 bps on top of that size drives cash to −49.97 (violating L10), so the L5 test passes `fee_bps=0.0` (the criterion states only slippage for L5). Builder must pick ONE insufficient-cash strategy for `Ledger`; whichever it picks, the branch logic accepts it. | P4-T1..T6 test-author agent (Buffy) | Open |
