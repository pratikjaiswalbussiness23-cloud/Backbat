# verified_apis.md

Per ROADMAP.md §2A (R1): never assume a third-party API exists. Before using any function/class from a library, verify it against the INSTALLED version (inspect.signature, help(), installed source, or official docs) and record it here with the package version.

| Package | Version | Function/Class | Verified via | Notes |
|---------|---------|----------------|--------------|-------|
| pydantic-settings | 2.15.0 | `pydantic_settings.BaseSettings` | `uv run python -c "from pydantic_settings import BaseSettings; print(BaseSettings.__module__)"` → `pydantic_settings.main` | P0-T4 mandated pre-check. Correct import path for v2; NOT `pydantic.BaseSettings` (removed in pydantic v2) |
| pydantic-settings | 2.15.0 | `pydantic_settings.SettingsConfigDict` | import + `SettingsConfigDict.__annotations__` | Supports `env_prefix`, `extra`, `env_ignore_empty`; keys confirmed on installed version |
| pydantic-settings | 2.15.0 | `BaseSettings` + `Field(validation_alias="ENV_NAME")` | behavior probe: env set → value picked up; unset → default; empty string → default kept with `env_ignore_empty=True` | `pydantic_settings.EnvNameAlias` does NOT exist in 2.15.0 (ImportError); plain str `validation_alias` is the correct mechanism. Field names stay snake_case (`anthropic_api_key` ← `ANTHROPIC_API_KEY`). Without an explicit alias + `env_prefix=""`, env vars are matched by FIELD name only — bare `offline`/`max_bars`, never `NLBT_OFFLINE`/`NLBT_MAX_BARS` (caught by the env-path tests) |
| pydantic-settings | 2.15.0 | `extra="forbid"` semantics | empirical probe + installed source `sources/providers/env.py` | Rejects unknown *constructor kwargs*. Does NOT flag unknown *env vars* (env source is field-driven) — recorded as OQ-0002 |
| pydantic | 2.13.5 | `pydantic.__version__` | `uv run python -c "import pydantic; print(pydantic.__version__)"` | P0-T4 mandated pre-check |
| pydantic | 2.13.5 | `BaseModel.model_config` dict style (`extra="forbid"`) | probe: class with dict `model_config` instantiates | Not used in P0-T4 (errors are plain exceptions); verified for later spec models (P3) |
| stdlib logging | CPython 3.14.4 | `logging.Filter` / `logging.StreamHandler.setStream` | `inspect.signature(logging.Filter.__init__)` → `(self, name='')`; `hasattr(StreamHandler, 'setStream')` → True | Filter signature stable; setStream used to capture output in tests |
| pandas | 3.0.6 | `pd.read_csv` | `inspect.signature(pd.read_csv)` — params confirmed: `dtype`, `parse_dates`, `index_col`, `usecols`, `na_filter` all exist | P1-T2 mandated pre-check. We pass only `dtype="str"` + `Path`; dates parsed explicitly afterwards |
| pandas | 3.0.6 | `pd.to_datetime(series, format="ISO8601", errors="coerce", utc=True)` | behavior probe: `"not-a-date"` → `NaT` (coerce); valid ISO → tz-aware UTC `Timestamp`; works on `str`/`string` dtype columns | `format="ISO8601"` accepted on 3.0.6; explicit parse per P1-T2, never inference |
| pandas | 3.0.6 | tz-aware comparisons | behavior probe: comparing tz-naive `Timestamp` with tz-aware `DatetimeIndex` raises `TypeError: Cannot compare tz-naive and tz-aware...` | pandas 3 strictness — date-range filters must build boundaries with `pd.Timestamp(date, tz="UTC")` |
| pandas | 3.0.6 | `DataFrame` subclassing (`_metadata` + `_constructor`) | behavior probe: subclass survives `[]`/`astype`/slicing ops; `isinstance(result, Bars)` stays True; `attrs` dict usable | `Bars` contract relies on this documented subclass contract |
| pandas | 3.0.6 | `pd.api.types.is_numeric_dtype` | probe: True for `float64`, False for `string` dtype | dtype gate in `validate_bars_shape` |
| pandas | 3.0.6 | `read_csv` blank-line behaviour | probe: blank lines skipped by default (`skip_blank_lines=True`); empty FIELD inside a row → NaN even with `dtype=str` | NaN detection targets fields, not blank lines |
| yfinance | 1.7.0 | `yf.download` | `inspect.signature(yf.download)` — confirmed params: `tickers, start, end, actions, threads, ignore_tz, group_by='column', auto_adjust=True, back_adjust, repair, keepna, progress, period, interval, prepost, rounding, timeout, session, multi_level_index` | P1-T3 mandated pre-check. We pass: `tickers, start, end, interval, auto_adjust, progress=False, threads=False` |
| yfinance | 1.7.0 | download return STRUCTURE (single ticker) | REAL probe run 2026-09-19: `yf.download('AAPL', start='2024-01-02', end='2024-01-06', auto_adjust=True)` → columns are a **MultiIndex** `[('Close','AAPL'), ('High','AAPL'), ('Low','AAPL'), ('Open','AAPL'), ('Volume','AAPL')]` (names `['Price','Ticker']`); dtypes: OHLC `float64`, **Volume `int64`**; index: `DatetimeIndex` name `'Date'`, dtype `datetime64[s]`, **tz-naive** (already ascending); with `auto_adjust=True` NO `Dividends`/``Stock Splits` columns appear | Do NOT assume: column structure changed across yfinance versions. Provider must flatten level 0, lowercase, keep only the 5 OHLCV columns |
| yfinance | 1.7.0 | `end` parameter is EXCLUSIVE | probe: `end='2024-01-06'` returned last row `2024-01-05` | nlbt's DataProvider contract is INCLUSIVE `[start, end]` → provider passes `end + 1 day` to yfinance, then defensively slices |
| yfinance | 1.7.0 | `Ticker.history` | exists on `Ticker('AAPL')`; wrapped by a decorator so `inspect.signature` shows `(*args, **kwargs)` — real signature read from installed source; parameters documented on the class docstring | We use `download` (documented wrapper) instead of `Ticker.history` — same data, simpler batching |
| requests | 2.34.2 | `requests.exceptions.RequestException` | import + class lookup on installed version | network-failure catch boundary for the retry loop (yfinance uses requests) |
| pyarrow | 25.0.1 | parquet engine behind `pd.DataFrame.to_parquet` / `pd.read_parquet` | `import pyarrow; print(pyarrow.__version__)` (P1-T4 mandated check 1); parquet round-trip probe | Already a `[project]` dependency since ADR-0001 — no new dependency needed for P1-T4 |
| pandas | 3.0.6 | `DataFrame.to_parquet()` | `inspect.signature` (mandated check 2): `(path=None, *, engine='auto', compression='snappy', index=None, ...)`; returns `bytes` when `path=None` | Byte-determinism probed: two writes of the same frame → identical bytes (snappy); file-write == buffer-write bytes; index serialized (tz preserved, unit `us`) |
| pandas | 3.0.6 | `pd.read_parquet` | `inspect.signature` (mandated check 3): `(path, engine='auto', columns=None, ...)` — accepts `FilePath \| ReadBuffer[bytes]` | **Probe caught a trap:** raw `bytes` are REJECTED (`TypeError: cannot construct a FileSource`) despite the signature; must wrap in `io.BytesIO`. Garbage bytes raise `pyarrow.lib.ArrowInvalid`. `df.attrs` round-trips through parquet metadata (`nlbt_validated` survives) |
| stdlib hashlib | CPython 3.14.4 | `hashlib.sha256` | `hashlib.algorithms_guaranteed` (mandated check 4) contains `'sha256'`; digest hex length 64 | Cache-key + content-hash basis |
| pydantic-settings | 2.15.0 | `SettingsConfigDict(populate_by_name=True)` | behavior probe: `M(_env_file=None, offline="false", max_bars="5")` constructs via field-name kwargs while `validation_alias` still drives env lookup | Required so tests/CLI can construct `Config` with snake_case kwargs; without it, alias-only models reject field-name kwargs |
